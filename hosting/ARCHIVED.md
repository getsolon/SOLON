# hosting/ — ARCHIVED

This Next.js frontend has been superseded by the `dashboard/` React SPA, which now handles both local and cloud functionality including managed hosting.

**Do not add new features here.** All cloud UI development should happen in `dashboard/src/pages/cloud/`.

The `infra/` subdirectory (Ansible playbooks, Terraform modules) remains in active use by the provisioner service.
