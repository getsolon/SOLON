---
# roles/solon-managed/handlers/main.yml

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart solon
  ansible.builtin.systemd:
    name: solon
    state: restarted
    daemon_reload: true
